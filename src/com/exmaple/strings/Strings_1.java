package com.exmaple.strings;

public class Strings_1 {
    public static void strings() {
        //Call methods
    }


    private static String toUpper(String s, String toUpperCase) {
        return s.replaceAll(toUpperCase, toUpperCase.toUpperCase());
    }
}
